# Backend

Go HTTP API and event processor for GitActionFlow.

**Phase 1:** Directory reserved. No application code yet.

Planned contents (later phases):

- Go module and `main` entrypoint
- Gin (or equivalent) HTTP server
- Config loaded from environment
- pgx PostgreSQL access
- Auth, webhooks, rules, actions packages
- Tests

See root `README.md`, `AGENTS.md`, and `docs/architecture/HLA.md`.
